# Changelog

## Unreleased

## 1.0.2 - 2026-08-14

- Teach update tools to retain and verify the existing Broadcast schedule or
  workflow running status without broad searches or unbounded polling.

## 1.0.1 - 2026-07-31

- Add the verified `com.mailrith/mailrith` MCP Registry identity and local npm
  stdio installation metadata while retaining the canonical hosted endpoint.
- Restore Full Email Marketing Access as the submitted OAuth bootstrap default
  while keeping exact operation scopes on normal tools and exact call-time
  authorization.
- Read `MAILRITH_API_KEY` in the local CLI and let the public API enforce
  permissions for direct credential-backed runtimes that do not preload a
  capability response.
- Route every permanent delete through `mailrith_delete`, including deletes that also require Perform Live Actions.
- Use effective workspace capabilities for connection, operation-search, and exact-schema availability instead of checking permissions alone.
- Reuse the shared bounded intent catalog for common Subscriber status, delivery setup, Broadcast scheduling, data transfer, analytics, and capture tasks.

## 1.0.0 - 2026-07-26

- Make the fixed submitted profile the default hosted and package surface, with focused tools, exact input and output schemas, human-readable titles, explicit annotations, and per-tool OAuth scopes.
- Allow anonymous static discovery for platform linking while rejecting every anonymous tool call before product access.
- Return tool-level OAuth challenges for missing connections and narrow grants, and keep scoped API-key support for direct and code-first clients.
- Preserve the compact seven-tool and configurable focused profiles as explicit compatibility options.

## 0.2.0 - 2026-07-24

- Derive every permission and focused toolset from the final resource contract and complete Work Profiles.
- Replace the operation-per-tool surface with seven stable discovery and execution tools that load one exact schema on demand.
- Match normal action words, product synonyms, and singular or plural resource names through a bounded in-memory operation index.
- Mark close operation matches as ambiguous so agents narrow the task before execution.
- Diagnose missing access with exact permissions, recommended Work Profiles, and a direct access-update link.
- Keep live sends, workflow activation, Subscriber delivery or targeting changes, public capture surfaces, and outbound event delivery behind the centralized Live Actions permission.
- Add direct stable-ID retrieval and compact import and export job-list tools.
- Return bounded resource details, prerequisites, and retry guidance in structured errors.
- Add generated Sender Identity and Broadcast scheduling tools, omission-safe update inputs, and exact rich-object schemas.
- Use one standard hosted MCP URL with OAuth Work Profile selection, compact tool lists, internal task-focused filtering, and secure browser-based email delivery setup tools.
- Add delivery connection verification and real test-email tools without accepting provider secrets.
- Return effective workspace capabilities and actionable delivery prerequisites.
- Keep capability results aligned with internal task-focused toolsets and read-only filtering, and add bounded Sequence and Automation readiness, journey-preview, and test-message tools.
- Add Subscriber-aware Template and double-opt-in previews, personalized message tests, evaluated journeys, and compact database-backed resource discovery.
- Remove the pre-launch Agent Sandbox surface from capabilities and tool results.
- Remove the pre-launch Agent Activity tools and permission.

## 0.1.2 - 2026-07-24

- Report the published package version in the MCP server identity.
- Correct the public-registry clean-install release gate.

## 0.1.1 - 2026-07-24

- Republish the MCP server as part of the coordinated Agent Skill patch release.

## 0.1.0 - 2026-07-24

- Request the workspace and Subscriber read permissions required by the default read-only starter.
- Document the 20-subscription workspace limit for Webhook subscription creation.
- Use task-focused resource, subscription, bulk-operation, and Webhooks Read or Write permissions.
- Remove tools for recommendation records, reference experiments, and generic compliance events.
- Publish the first stable local and self-hosted MCP server.
- Add exact generated schemas, focused toolsets, risk annotations, OAuth discovery, and structured errors.
- Use the stable MCP TypeScript SDK with a stateless Web Standards transport for hosted requests.
