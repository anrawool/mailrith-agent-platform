# Changelog

## 0.1.0-beta.1 - 2026-07-22

- Publish the first local and self-hosted MCP server beta.
- Add exact generated schemas, focused toolsets, risk annotations, OAuth discovery, and structured errors.
- Use the split MCP TypeScript server package at `2.0.0-beta.5`, with a stateless per-request hosted handler and no Node HTTP adapter dependency.
